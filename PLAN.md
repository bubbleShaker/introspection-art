# 描画を p5.js の WEBGL + シェーダーに置き換える

[Issue #9](https://github.com/bubbleShaker/introspection-art/issues/9)

## なぜ置き換えるか

今の `src/scene/waterScene.ts` は Canvas2D で、空を一度オフスクリーンに描き、
それを 2px のスライスに切って横へずらしながら下半分へ加算転写している。
水に見せる工夫は入っているが、**CPU が 1 行ずつ絵を平行移動させる**方式なので、
表現できることがその範囲に閉じている。

- 反射が「横ずれ」でしかない（波の傾きに応じた屈折が無い）
- 光の柱を、短い横線を並べて似せている（実際に月を向いた波面が光っているわけではない）
- 滲みを太線と細線の重ね描きで偽装している（本物のブルームではない）
- 「遠いほど鏡らしく、手前ほど暗い」を `depth ** 1.25` のような手作業の係数で作っている

GPU で 1 ピクセルずつ計算する形にすると、これらは**作り込むものではなく、
物理から出てくるもの**になる。

## 設計の骨

**フルスクリーンの板 1 枚に、フラグメントシェーダーを 1 本貼る。**
3D モデルは置かない。ピクセルごとに視線を 1 本飛ばし、それが空に抜けるか
水面に当たるかで色を決める。

```
      ＼      ← 空へ抜ける視線 : skyColor(rd)
       ＼
  目 ●──────── 水平線
       ／＼
      ／  ＼  ← 水面に当たる視線
  ～～～～～～～ 水面 (y = 0)
```

肝は **`skyColor(dir)` という関数を、空の描画と水の反射で共用する**こと。
水面のある点で法線を求め、視線を反射させ、その方向の空の色を同じ関数で引く。
「空で起きたことは水面にも現れる」という作品の芯が、転写ではなく計算で成り立つ。

```glsl
vec3 skyColor(vec3 dir);   // 方向 → 空の色（グラデ・月・星・雲）

void main() {
  vec3 rd = 視線方向;
  if (rd.y >= 0.0) {
    col = skyColor(rd);                       // 空
  } else {
    vec3 p = 水面との交点;
    vec3 n = waterNormal(p.xz);               // 波の高さ場の勾配
    float f = fresnel(-rd, n);                // 浅い角度ほど鏡になる
    col = mix(WATER_DEEP, skyColor(reflect(rd, n)), f);
  }
}
```

`fresnel` が入るだけで、遠くの水面は鏡のように空を映し、手前は暗く沈む。
今の実装が係数で作っていた奥行きが、ここから自動的に出る。

## 何を残すか

音の解析層（`src/audio/`, `src/core/`）は**触らない**。
シーンへ `Levels`（低・中・高）を渡す口を変えないので、
帯域の切り出し・打点検出・波紋の減衰のテストはそのまま残る。

`RippleField` の純粋計算も残し、シェーダーへ uniform として送る側にする。

| 層 | 扱い |
| --- | --- |
| `src/audio/`, `src/core/` | 据え置き |
| `src/scene/ripples.ts` | 残す（uniform の供給元になる） |
| `src/scene/particles.ts` | 星をシェーダーで描くので、M3 で役目を見直す |
| `src/scene/palette.ts` | 色をシェーダーの定数へ移す。M6 で整理 |
| `src/scene/waterScene.ts` | 置き換え対象 |
| `src/main.ts` | シーンの生成箇所だけ差し替え（ループ構造は維持） |

## p5 の使い方

p5 は「キャンバスの用意・シェーダーの取り回し・uniform の受け渡し」を担当する。
絵の中身は GLSL 側にある。

- **インスタンスモード**で起動する（グローバル汚染を避ける）
- **`noLoop()` して、外から `redraw()` を呼ぶ**。`main.ts` の
  `requestAnimationFrame` ループを二重にしないため。音のスムージングと
  打点検出は今のまま `main.ts` に残る
- 板は `plane(width, height)` を標準の行列変換で描く。
  フラグメント側は `gl_FragCoord` から座標を作るので、板の大きさに依存しない

### 注意: GLSL のバージョン

p5 2.x は WebGL2 コンテキストを取る。ただし **`#version 300 es` を足してくれるのは
組み込みのシェーダーだけ**で、`createShader()` に渡したものには手が入らない
（着手前は逆に読んでいた。実際に動かして分かった）。

- `#version 300 es` と `precision` は**自分で、1 行目に**書く（前に何か置くと通らない）
- `attribute` / `varying` ではなく `in` / `out`
- `gl_FragColor` ではなく自前の `out vec4`
- `texture2D()` ではなく `texture()`
- `sample` は予約語なので変数名に使えない

## 進め方

- [x] **M1** 土台。p5 WEBGL を起動し、視線・水平線・空のグラデ・月・
      フレネル反射までを 1 本のシェーダーで描く。`WaterScene` と入れ替える
- [x] **M2** 水面。ノイズベースの高さ場、距離に応じた細部の減衰（エイリアシング対策）、
      月を向いた波面が光る鏡面反射
- [x] **M3** 空。星の瞬き、雲、天の川、水平線際の霞
      （オーロラは入れなかった。この曲の静けさに対して強すぎる）
- [x] **M4** 波紋。`RippleField` を uniform で送り、波の高さ場に足す
- [x] **M5** ブルーム。明部抽出 → ぼかし → 加算のポストプロセス
- [x] **M6** 負荷調整（モバイルの描画解像度、`prefers-reduced-motion`、
      WebGL2 が無い環境の扱い）とドキュメント更新

各段でいったん見られる状態にし、`npm run build` と `npm test` を通してから次へ進む。

---

# 円形の波形を、立体の球にする

[Issue #13](https://github.com/bubbleShaker/introspection-art/issues/13)

## なぜ置き換えるか

波形は今、画面中央の**平らな白いリング**として描いている。3 パスが終わった
あとに `beginShape()` で線を引く「後乗せの飾り」なので、月明かりを受けず、
水面にも映らず、ブルームの段より後ろなので滲みも乗らない。
背景が GPU で物理から作られているのに対して、**リングだけが別世界にいる**。

## 設計の骨

p5 の `sphere()` ジオメトリは使わない。使うと世界が二重になり、
「空で起きたことは水面にも現れる」という芯が切れる。

`scene.frag.glsl` の視線計算に、**レイと球の交差判定を 1 本足す**。

```
      ＼      ← 空へ抜ける視線 : skyColor(rd)
       ＼  ●  ← 球に当たる視線 : sphereColor(...)
  目 ●──────── 水平線
       ／＼
      ／  ＼  ← 水面に当たる視線（反射の先でも球を見る）
  ～～～～～～～ 水面 (y = 0)
```

球は水面のすぐ上に浮かぶ 1 個。視線が水面に届く前に当たったらそちらを描く。
表面の波は**高さ場の勾配で法線を曲げる**だけで、形そのものは動かさない
（水面と同じやり方。レイマーチしないので交差は二次方程式 1 本で済む）。

波の中身は二つの重ね合わせ:

| 層 | 何を表す | 出どころ |
| --- | --- | --- |
| 3D ノイズを時間で流したうねり | 流動 | シェーダー内の `fbm3` |
| 波形を球へ巻きつけた帯 | 音への反応（旧リングの役目） | `waveSphere.ts` → uniform |

色は `skyColor()` を共用する。月を映し、フレネルで縁が白く光る。
ブルームの**前**の段なので、波頭の滲みは自動で乗る。

## 進め方

- [x] **M7a** 球を置く。交差判定・フレネル・月の鏡面反射まで（表面はまだ滑らか）
- [x] **M7b** 表面の波。3D ノイズのうねりを時間で流す
- [x] **M7c** 波形を球へ巻きつける。`waveRing.ts` を `waveSphere.ts` へ
- [x] **M7d** 水面に映す。リングの描画コードを畳み、README / summary を直す

## 気にすること

- **uniform の本数**: 波形 128 点は `vec4[32]` に詰めて渡す（float を 128 本
  並べると、実装によっては上限に触る）
- **負荷**: 球の色を決める関数が呼ばれるのは 1 ピクセルにつき最大 2 回
  （直接 + 水面の反射）。画面に占める面積は小さいので許容する
  （→ M8 で表と裏を重ねて最大 4 回になり、M9 で水面の反射が消えて最大 2 回に戻った）
- **`prefers-reduced-motion`**: 流動の速さに `uMotion` を掛ける

## 気にすること（M1–M6）

- **バンドル**: 置き換え前は 14.4kB (gzip 5.7kB)。p5 で大きく増える
- **モバイルの GPU 負荷**: 描画バッファの解像度を落として逃がす（M6）
- **エイリアシング**: 遠方の水面は 1 ピクセルに何十もの波が入る。
  距離に応じて高周波を殺さないと、水平線際がちらつく（M2 の主題）
- **`prefers-reduced-motion`**: 今は動きを 0.3 倍にしている。同じ配慮を引き継ぐ
- **README**: 「スライス転写で水を作る」という説明は書き直しになる（M6）

## M8: 球を格子にして透かす（[Issue #15](https://github.com/bubbleShaker/introspection-art/issues/15)）

面のある塊をやめ、緯線と経線の骨組みだけにした。線の隙間からは背後の海と
空が透け、向こう側の面の線も重ねるので中が空洞に見える。波形は面の凹凸では
なく、骨組みの歪みとして現れる。

M7 の図の「球に当たる視線 : `sphereColor(...)`」は、いまは
`sphereWire(...)`（表と裏の格子を重ねた色と覆い）に置き換わっている。
`sphereColor()` / `sphereWave()` / `sphereNormal()` は無い。

- [x] `main()` を「背景を決め切る → 格子を覆いのぶんだけ重ねる」順に組み替える
- [x] `sphereHit()` に `farSide` を足し、向こう側の面も描く
- [x] 線の太さを、距離と傾きから出した 1 ピクセルの覆いで滲ませる
- [x] 潰れた線の濃さを落とし、極と輪郭で白い塊に固まるのを防ぐ

詳細は `summary/260818-wire-sphere.md`。

## M9: 格子を白い線だけの手前のレイヤにする（[Issue #17](https://github.com/bubbleShaker/introspection-art/issues/17)）

M8 の線は、まだ空を映して月に照らされる「世界の中の物体」だった。
世界と噛み合う経路を全部外し、海と空の手前に重なるだけの白い線画にした。

- [x] 疎に・細く（経線 20 → 12、緯線 12 → 8、太さ 0.010 → 0.005）
- [x] `wireColor()` を廃し、白の一定色に。`sphereWire()` が返すのは覆いだけになった
- [x] 水面の反射レイから球の交差判定を外す（水面が映すのは空だけ）
- [x] 線の明るさをブルームの閾値の下に置き、線が滲まないようにする

M7 / M8 の節に残る次の記述は、この節で無効になっている。

- 図の「反射の先でも球を見る」— 水面が映すのは空だけ
- 「色は `skyColor()` を共用する。月を映し…波頭の滲みは自動で乗る」— 線は
  世界の光を受けず、ブルームにも拾われない
- 「`sphereWire(...)`（表と裏の格子を重ねた色と覆い）」— 返すのは覆いだけ

詳細は `summary/260818-white-wire.md`。

## M10: 綺麗な球を、平行な格子で包んで回す（[Issue #19](https://github.com/bubbleShaker/introspection-art/issues/19)）

M8/M9 の格子は、波形が半径を膨らませ、3 次元ノイズが線を押し曲げていたので、
輪郭も線もヨレて見えていた。線は緯線・経線で、経線は必ず上下の極へ集まっていた。

- [x] 半径から波形のヨレを、格子から `fbm3` のうねりを外す（形は完全な球に）
- [x] 緯度経度をやめ、**互いに平行な平面で球を切った円**の二組にする（極が無い）
- [x] 線を半透明にする（`WIRE_ALPHA`）
- [x] 格子をゆっくり回す。**視線の軸まわりだけを回し切り**、他の 2 軸は揺らすに留める
- [x] 音への反応を、線の濃さ・一様な呼吸・回る速さの三つへ移す

M7–M9 の節に残る次の記述は、この節で無効になっている。

- 「波形を球へ巻きつける」— 巻きつけた輪が動かすのは線の濃さで、形ではない
- 「緯線と経線の骨組み」「緯線を上下に押してうねらせる」— 緯度経度は使っていない
- 「`sphereSurfaceHit()` で 2 段階に切り直す」「`SPHERE_SWING`」— 半径は向きに依らない
- 「3D ノイズを時間で流したうねり ｜ 流動 ｜ シェーダー内の `fbm3`」— `fbm3` ごと
  落とした（`hash31` / `valueNoise3` も一緒に消えている）
- 「線が覆いきったところの色は `WIRE_COLOR` そのもの」— 半透明なので背後が透ける

詳細は `summary/260818-parallel-grid.md`。
